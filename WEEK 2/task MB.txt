#include <iostream>
using namespace std;

main( ) 
{
    double mb, bits;

    cout << "Enter the size in             megabytes (MB): ";
    cin >> mb;

    cout << mb << " MB is equivalent to " << bits << " bits" << endl;

    return 0;
}