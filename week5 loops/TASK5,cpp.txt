#include <iostream>
using namespace std;

int main()
 {
    int sum = 0;
    number = abs(number);
    while (number > 0) {
        sum += (number % 10);
        number /= 10;
    }
    return sum;
 {
    int num;
    cout << "Enter a number: ";
    cin >> num;
    cout << "Sum of digits: " << digitSum(num) << endl;
    
}
}